import { memo } from 'react';
import { IntegrationsOverlay } from './integrations-overlay';

export const ArtifactIntegrations = memo(function ArtifactIntegrations() {
  return <IntegrationsOverlay />;
});
